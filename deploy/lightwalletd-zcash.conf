# lightwalletd reads node RPC connection details from this bitcoind-style conf.
# Zebra's RPC does not require auth; the user/password are placeholders lightwalletd expects.
testnet=1
rpcbind=zebra
rpcport=18234
rpcuser=zecd
rpcpassword=zecd-localnet
