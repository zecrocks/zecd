# cargo-audit configuration (for local `cargo audit` runs; CI uses `cargo deny`,
# whose deny.toml documents the same advisory exceptions).

[advisories]
# No advisory exceptions are currently needed: RUSTSEC-2026-0009 (`time` <0.3.47) was
# dropped when the librustzcash 0.24-rc line removed the `time-core = "=0.1.2"` pin that
# had held `time` back at 0.3.37.
ignore = []
