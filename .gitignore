/target
/regtest-harness/target
**/*.rs.bk
Cargo.lock.orig

# Local runtime data / wallets (never commit secrets)
/data
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm
keys.toml
*.cookie
.cookie
identity.txt
zecd.toml
# ...but the docker-stack template is intentional (a checked-in example, no real secrets).
!/deploy/zecd.toml
__pycache__/
